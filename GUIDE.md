# SoDamContext — 왕초보 단계별 가이드 👶

> 컴퓨터·AI·메신저·스마트폰이 **처음**인 분도 그대로 따라 할 수 있게, **"지금 이걸 누르세요"** 식으로 설명합니다.
> (간단 요약·명령어·아키텍처·라이선스 전체는 같은 폴더의 `README.md` 에 있어요.)

> ⚠️ **정직한 안내(2026-07-15 기준):** 지금 **작동까지 확인된 기능은 검진(점검)·문진(만들기)·처방(다듬기)·동기화(두 설명서 비교)** 네 가지예요 — **클로드코드·코덱스 둘 다 실제로 써서 확인 완료**했어요. **정기 점검 알림·참고 점수(건강점수)**는 코드를 다 만들고 자동 시험·명령줄 직접 실행으로 확인했지만, 실제 대화 화면에서 자연스럽게 나오는지는 아직 추가 확인 전이에요. **예방(저장 전 자동 차단)은 코드는 다 만들어져서 부품 하나하나는 정확히 작동함을 확인했지만, 실제 저장 화면에서 확인창이 뜨는지는 아직 검증 중**이에요 — 그래서 비밀키는 지금도 직접 한 번 더 확인해 주세요. 이 도구는 **컴퓨터(Windows·macOS)** 에서 돌아가요 — 휴대폰 앱이 아니에요.

---

## 목차

- [먼저 알아둘 것](#먼저-알아둘-것)
- [1단계 준비물 확인](#1단계-준비물-확인)
- [2단계 설치하기](#2단계-설치하기)
- [2-B단계 코덱스 수동 설치](#2-b단계-코덱스-수동-설치)
- [3단계 껐다 켜기](#3단계-껐다-켜기)
- [4단계 설명서 만들기](#4단계-설명서-만들기)
- [5단계 점검하기](#5단계-점검하기)
- [6단계 다듬기](#6단계-다듬기)
- [6-B단계 두 설명서 맞추기(동기화)](#6-b단계-두-설명서-맞추기동기화)
- [7단계 막혔을 때](#7단계-막혔을-때)
- [8단계 안전 약속](#8단계-안전-약속)
- [업데이트 내용 요약](#업데이트-내용-요약)
- [자주 묻는 질문](#자주-묻는-질문)
- [라이선스 안내](#라이선스-안내)

---

## 먼저 알아둘 것

1. 이 도구는 **클로드코드 안에서** 작동해요. (클로드코드 = AI랑 글로 대화하며 코딩하는 컴퓨터 프로그램)
2. 한 번만 **설치**하면 됩니다. 그다음부턴 명령어만 치면 돼요.
3. **"입력창"** = 클로드코드에서 글을 치는 칸. 이 가이드에서 "입력창에 ~치세요"는 거기에 친다는 뜻이에요.

> 💡 "AI 설명서"란? AI가 일 시작 전에 읽는 `CLAUDE.md`·`AGENTS.md` 파일이에요. "이 프로젝트는 이렇게 다뤄줘"라고 적어 두는 쪽지랍니다.

---

## 1단계 준비물 확인

입력창에 아래를 치고 Enter:
```
!node --version
```
- **`v18` 이상**(예: v20, v22) 숫자가 나오면 → 준비 끝 ✅
- 빨간 글씨/`없음`이 나오면 → 인터넷 브라우저로 **nodejs.org** 접속 → 초록색 **LTS** 버튼 눌러 설치 → 컴퓨터 다시 시작.

(코덱스도 쓰려면 `!git --version` 도 쳐서 숫자가 나오는지 확인하세요. 없으면 **git-scm.com** 에서 설치.)

---

## 2단계 설치하기

입력창에 **아래 두 줄을 한 줄씩** 붙여넣고 각각 Enter:
```
/plugin marketplace add sodam-ai/SoDam-Context-Eng
```
```
/plugin install sodam-context
```
- "설치됐어요" 비슷한 메시지가 나오면 성공이에요.
- *(내 컴퓨터 폴더로 직접 설치하는 경우엔, 첫 줄의 `sodam-ai/SoDam-Context-Eng` 자리에 그 폴더 경로를 넣으세요.)*

---

## 2-B단계 코덱스 수동 설치

코덱스(OpenAI Codex)는 플러그인 마켓이 없어서 **저장소를 통째로 받아 그 안에서** 씁니다.
(스킬 폴더만 복사하면 검진 도구가 쓰는 `lib`·`rules` 폴더가 빠져서 "파일을 찾을 수 없어요" 오류가 나요.)

1. **저장소 받기** — 터미널(명령 프롬프트)에 한 줄씩:
   ```
   git clone https://github.com/sodam-ai/SoDam-Context-Eng.git
   cd SoDam-Context-Eng
   ```
   *(이미 받아둔 폴더가 있으면 그 폴더로 이동하면 돼요.)*

2. **코덱스를 이 저장소 루트 폴더에서 시작**하세요. (루트에 `AGENTS.md`·`skills`·`lib`·`rules`가 다 있어요.)
   ⚠️ 루트가 아닌 다른 폴더에서 실행하면 "파일을 찾을 수 없어요" 오류가 나요.

3. **코덱스에서 자연어로 시험**:
   ```
   내 AGENTS.md 건강검진 해줘
   ```
   - 검사할 파일은 **절대경로**로 알려주세요 (예: `C:\내프로젝트\AGENTS.md`).
   - *(코덱스는 `/명령어` 방식이 아니라 자연어로 스킬이 동작해요.)*
   - 더 자세한 설치·FAQ는 저장소의 **`codex/README.ko.md`** 를 참고하세요.

> **한글 용량 주의**: 한글 1글자 = 3바이트예요. 짧아 보여도 실제 용량이 커서 코덱스 32 KiB 한도에 걸릴 수 있어요. 검진 시 자동으로 알려드려요.

---

## 3단계 껐다 켜기

방금 설치한 건 **다시 켜야** 나타나요.
- 입력창에 `exit` 치고 Enter → 창을 **X로 완전히 닫기**
- 다시 클로드코드를 켜기 (터미널이면 `claude` 입력)

확인: 입력창에 `/` 만 쳐보세요. 목록에 **`sodam-context-checkup`**·**`sodam-context-intake`**·**`sodam-context-treat`**·**`sodam-context-sync`** 가 보이면 준비 완료! 🎉

---

## 4단계 설명서 만들기

아직 AI 설명서(`CLAUDE.md`)가 없다면 만들어 봅시다. (이 기능 이름 = **문진**)

1. 입력창에 치기:
   ```
   /sodam-context-intake
   ```
2. "어느 폴더에 만들까요?"라고 물으면 → 만들 폴더 경로를 알려주거나 **"지금 이 폴더"** 라고 답하기.
3. AI가 쉬운 질문을 **하나씩** 해요. 막히면 이렇게 답해도 돼요:

   | 질문 | 이렇게 답하면 돼요 |
   |---|---|
   | 무슨 프로젝트예요? | `그냥 연습용` |
   | 무슨 언어·도구로? | `모름` |
   | 꼭 지킬 규칙은? | `한국어로 답해줘` |
   | 하면 안 되는 건? | `비밀번호를 코드에 적지 마` |
   | 말투는? | `쉽게, 짧게` |

4. AI가 **만들 내용을 먼저 보여주고** "이대로 만들까요?"라고 물어요.
   → 마음에 들면 **`네`** 라고 답하면 그때 파일이 생겨요. (묻기 전엔 절대 안 만들어요)
5. "잘 만들었어요!" 가 나오면 끝. 이어서 "검진도 해볼까요?"라고 물으면 `네` 하면 점검까지 해줘요.

---

## 5단계 점검하기

이미 `CLAUDE.md`가 있는 폴더라면: (이 기능 이름 = **검진**)

1. 입력창에 치기:
   ```
   /sodam-context-checkup
   ```
2. "어느 파일을 볼까요?"라고 물으면 → 그 파일 경로를 알려주기 (예: `C:\내폴더\CLAUDE.md`).
3. 잠시 후 **쉬운 리포트**가 나와요:
   - "문제 ○건" 처럼 **개수**가 가장 먼저 나와요
   - 비밀번호/키가 있으면 **가린 채**(`sk-ant-…REDACTED`)로만
   - 낡음·미정제 같은 항목은 **"의심"** 으로(확정 아니라 참고용)
   - **참고 점수**(예: "85점")가 개수 뒤에 보조로 붙어요 — 항상 **"참고용(검증 전)"** 이라는 말이 같이 나와요(아직 여러 사람 데이터로 다듬은 공식 점수가 아니라는 뜻)
   - 마지막으로 점검한 지 **30일이 넘었으면** "다시 점검해볼까요?" 정도로 짧게 안내해요(자동 알림이 아니라, 점검을 실행하는 그 순간에만 비교해요. 30일이 안 지났으면 이 말 자체가 안 나와요)
   - 더 궁금하면 **`왜요?`** 라고 답하면 이유를 설명해줘요.

---

## 6단계 다듬기

검진에서 문제를 찾았다면 안전하게 다듬을 수 있어요. (이 기능 이름 = **처방**, 실사용 확인 완료)

1. 입력창에 치기:
   ```
   /sodam-context-treat
   ```
2. **백업을 먼저** 뜨고, **무엇을 바꿀지 먼저 보여줘요.**
3. **`네`** 라고 해야만 실제로 고쳐요(중복 줄·불필요한 빈 줄 정리).
4. 마음에 안 들면 **되돌리기**로 백업에서 원래대로 복구할 수 있어요.
5. **안전·금지 규칙 줄**(예: "절대", "never", "비밀번호 금지")은 **자동으로 보존**돼요(안 지워요).
6. **엉뚱한 위치는 아예 못 건드려요:** 컴퓨터의 중요한 폴더(예: 로그인 정보가 든 폴더, 시스템 폴더)를 실수로 지정해도, 도구가 미리 알아채고 **거부**해요.

> ⚠️ 처방은 실제로 써서 확인까지 끝냈어요. 그래도 처음 쓸 땐 **중요한 파일은 따로 복사**해 두는 걸 권장해요.

---

## 6-B단계 두 설명서 맞추기(동기화)

**클로드코드용(`CLAUDE.md`)과 코덱스용(`AGENTS.md`)을 둘 다 쓰신다면**, 이 단계로 두 파일이 서로 다른 안전 규칙을 갖고 있지 않은지 확인할 수 있어요. (이 기능 이름 = **동기화**, 실사용 확인 완료)

1. 입력창에 치기:
   ```
   /sodam-context-sync
   ```
2. 비교할 두 파일의 경로를 알려주세요(예: `CLAUDE.md`와 `AGENTS.md`).
3. 결과로 **"이 규칙은 클로드용에만 있어요(○번째 줄)"** 처럼 알려줘요.
4. **파일을 대신 고치진 않아요** — 어느 쪽에 맞출지는 사용자가 직접 정해서 손으로 옮기면 돼요. 실수로 다른 내용까지 바뀌는 위험을 막기 위한 설계예요.

> 💡 하나의 AI 도구만 쓰신다면(클로드코드만, 또는 코덱스만) 이 단계는 건너뛰어도 괜찮아요.

---

## 7단계 막혔을 때

당황하지 마세요. 아래 표대로 하면 돼요.

| 이런 일이 | 이렇게 하세요 |
|---|---|
| `/sodam-context-...` 가 목록에 없어요 | 3단계(껐다 켜기) 다시. 그래도 없으면 입력창에 `! claude plugin marketplace update sodamcontext-marketplace` 치고 다시 껐다 켜기 |
| 엉뚱한 게 답해요 | 그냥 말로 하지 말고 꼭 **`/sodam-context-...`** 로 시작하는 명령을 쓰기 |
| "node 없음" 이라고 떠요 | 1단계로 가서 Node.js 설치 |
| 묻지도 않고 파일을 만들/고쳤어요 | 🚨 멈추고 도와줄 사람에게 알리기 (원래는 꼭 물어봐야 정상) |
| 코덱스에서 "Cannot find module" 오류 | 코덱스를 **저장소 루트 폴더**(`SoDam-Context-Eng`)에서 다시 실행하기 |
| "왜요?" 버튼이 없어요 | CLI에는 버튼이 없어요. `왜요?` 라고 그냥 입력하면 이유를 설명해줘요 |
| 점수가 안 나와요 | 의도적으로 숨겼어요. "문제 ○건"으로만 봐요 (과장 방지) |
| 한글이 깨져요 | Node.js v18 이상인지 확인. Windows 터미널 인코딩이 UTF-8인지도 확인해요 |
| 비밀번호가 있대요, 근데 실제로 없는 것 같아요 | 정규식은 비슷하게 생긴 것도 "의심"으로 잡아요. 자동 삭제는 안 하니까 직접 확인하고 결정하면 돼요 |
| 자동 허용 모드(권한 모드)에서 써도 안전한가요? | 자동 허용 모드에서는 확인 없이 넘어갈 수 있어요. 안전을 위해 기본(ask) 모드를 권장해요 |
| 검진 도중 갑자기 멈췄어요 | 원본 파일은 건드리기 전에 중단하도록 설계됐어요. 원본은 그대로예요 |
| 되돌리기가 "백업 파일 위치가 아니에요"라며 안 돼요 | 지정한 파일이 이 도구가 만든 진짜 백업이 아니에요(정상적인 안전 동작이에요) | 6단계에서 안내받은 백업 경로를 그대로 사용해 주세요 |

---

## 8단계 안전 약속

이 도구가 지키는 약속이에요. 안심하세요.

- 🔒 **비밀번호·키:** 내용을 **안 보고** "있다"까지만 확인하고 **가려서** 보여줘요. **자동으로 안 지워요.**
- ✋ **확인 먼저:** 사용자가 **"네"** 하기 전엔 파일을 **만들지도 고치지도** 않아요.
- 🌐 **인터넷 안 씀:** 검진·문진·처방은 **내 컴퓨터 안에서만** 돌아가요(설치할 때만 인터넷 필요).
- ♻️ **안전하게 고침:** 처방은 백업을 먼저 뜨고, 임시파일에 쓴 뒤 바꿔치기해서 도중에 멈춰도 원본이 안 깨져요.
- 🔍 **한 파일만 봐요:** 코덱스는 상위 폴더까지 합쳐서 보지만, 클로드코드는 지금은 **지정한 파일 하나만** 봐요. 여러 폴더에 규칙을 나눠 두셨다면 각각 따로 점검해 주세요.
- 🛑 **저장 전에 미리 막도록 만들어 뒀어요:** 설명서에 진짜 비밀키가 들어가거나 너무 길어지면(300줄/32KB 이상) 저장 전에 막고, 애매하게 긴 정도(200~299줄)면 "그래도 저장할까요?"라고 묻도록 설계돼 있어요. **다만 이 확인창이 실제로 뜨는지는 아직 검증 중**이라, 지금은 비밀키가 없는지 직접 한 번 더 봐 주세요.
- 🔗 **동기화는 알려만 줘요:** 클로드용·코덱스용 설명서 중 한쪽에만 있는 안전 규칙을 찾아서 줄 번호로 알려드려요. **파일을 대신 합치거나 고치진 않아요** — 실수로 다른 내용까지 바뀌면 위험하니까요.
- 🛡️ **되돌리기 소스도 확인해요(2026-07-13 추가):** "되돌리기"는 이 도구가 실제로 만든 백업 폴더 안의 파일만 써요. 다른 아무 파일이나 백업인 척 지정해도 거부돼요 — 엉뚱한 내용이 실수로 설명서에 복사되는 걸 막기 위해서예요.
- 🗓️ **정기 점검 알림도 내용은 저장 안 해요(2026-07-15 추가):** "언제 마지막으로 점검했는지" 기록은 파일 경로랑 시각(날짜·시간)만 저장해요. 설명서에 뭐라고 적혀 있는지는 이 기록에 전혀 안 들어가요.
- 🔢 **참고 점수는 숨김 계산이 아니에요(2026-07-15 추가):** 점수를 만들려고 파일을 다시 읽지 않아요. 검진이 이미 찾아낸 문제 개수로 그 자리에서 계산하고, 계산 방법(확정 -15점·의심 -5점)도 전부 공개해요.
- 🙇 **"무조건 안전" 장담 안 해요:** 정해진 형태만 잡으니, 중요한 비밀번호는 직접 챙기세요.

> 데이터 흐름(쉽게): 원본 파일은 도구가 **속으로만** 읽고, AI에게는 **요약(비밀키 값 없음)** 만 전달돼요. 그래서 비밀키가 새지 않아요.

---

## 업데이트 내용 요약

<details>
<summary><b>📋 눌러서 펼치기 — 무엇이 바뀌었나</b></summary>

**0.1.0 이후 (진행 중, 2026-07-15)**

- **"마지막 점검이 언제였는지" 기억해 줘요:** 30일 넘게 점검을 안 했으면, 다음 점검할 때 "다시 점검해볼까요?"라고 짧게 알려줘요. 내용은 저장 안 하고 날짜만 기억해요.
- **참고 점수(건강점수)가 생겼어요:** 검진 결과 맨 뒤에 "85점" 같은 참고 점수가 보조로 붙어요. 아직 여러 사람 데이터로 검증된 공식 점수가 아니라서 항상 "참고용"이라고 표시돼요.
- **예전에 "점수 절대 안 보여준다"고 적어뒀던 안전 규칙**이 위 참고 점수 기능과 어긋나 있던 걸 발견해서 바로잡았어요.
- **자동 시험 134개로 확대**, 전부 통과 확인했어요.

**0.1.0 이후 (진행 중, 2026-07-13)**

- **보안 점검에서 실제 문제 하나 발견해 바로 고쳤어요:** "되돌리기" 기능에 백업 아닌 임의 파일을 지정하면 그 내용이 그대로 복사되던 걸 발견해서, 진짜 백업 폴더의 파일만 되돌리기에 쓸 수 있게 막았어요.
- **다른 드라이브에서 처방·되돌리기가 안 되던 문제 수정:** 프로젝트가 C드라이브가 아닌 곳(예: D드라이브)에 있으면 실패하던 걸 실제 사용자분이 겪으셔서 발견하고 고쳤어요.
- **문서에서 서로 다르게 적혀있던 내용을 통일했어요:** 예방·동기화 기능의 "지금 되는지 안 되는지"를 문서마다 다르게 적어놨던 걸 정확하게 통일했어요.
- **자동 시험 118개로 확대**, 전부 통과 확인했어요.

**0.1.0 이후 (진행 중, 2026-07-11)**

- 처방·코덱스 검진을 **실제로 써서 확인** 완료했어요(그동안은 자동 시험만 통과한 상태였어요).
- **새 안전장치:** 컴퓨터의 중요한 폴더(로그인 정보·시스템 폴더)를 실수로 지정해도 도구가 미리 알아채고 거부해요.
- 코덱스를 특별한 설정으로 쓰는 경우에 생길 수 있던 검진 오류를 고쳤어요.
- 라이선스를 Apache 2.0으로 최종 확정했어요.
- 자동 시험을 67개→**82개**로 늘려서 더 꼼꼼히 확인해요.

**0.1.0 이후 (2026-07-07)**

- 검진이 **"낡음·미정제·스킬누수"** 도 "의심"으로 잡아줘요(줄 번호와 함께).
- 처방 **되돌리기** 오류를 고쳤어요(이제 제대로 복구돼요).
- 코덱스 설치 방법을 **실제로 되는 방식으로 통일**했어요.
- README·가이드(한/영)를 지금 능력에 맞게 **정직하게** 갱신, PDF는 빼고 HTML로 대체.

**0.1.0 (2026-06-28) — 첫 공개**

- 검진·문진·처방 기본 기능, 코덱스 지원(병합체인·용량 확인), 한/영 문서, Apache-2.0 라이선스.

</details>

---

## 자주 묻는 질문

**Q. 내 파일이 인터넷으로 나가나요?** → 아니요. **내 컴퓨터 안**에서만 돌아가요.

**Q. 비밀번호를 자동으로 지워주나요?** → 아니요. "있다"까지만 확인하고 **가려서** 보여줄 뿐이에요. 직접 재발급하세요.

**Q. 스마트폰으로 되나요?** → 아니요. **컴퓨터(Windows·Mac)** 의 클로드코드·코덱스 안에서만 돼요.

**Q. 클로드코드랑 코덱스 같이 써도 되나요?** → 네. 클로드코드는 `CLAUDE.md`, 코덱스는 `AGENTS.md`를 읽어요. 같이 있어도 괜찮아요.

**Q. 돈이 드나요?** → 이 도구 자체는 무료로 돌지만, **AI 사용 요금(클로드·코덱스)** 은 각 제공사 정책에 따라요.

**Q. 참고 점수(건강점수)를 믿어도 되나요?** → "참고용(검증 전)"이라는 말이 항상 같이 나오는 이유가 그 답이에요 — 아직 공식 점수가 아니라 **보조 참고용**이에요. 진짜 정보는 "문제 ○건"이고, 점수는 그 뒤에 살짝 덧붙는 거예요.

---

## 라이선스 안내

> ⚖️ **이 안내는 법률 자문이 아닙니다.** 공개·배포·상업적 사용 전 **본인 확인 및 법무 검토**가 필요합니다.

- **라이선스: Apache License 2.0** © 2026 **SoDam AI Studio**. 수정·복제·재배포·**상업적 사용**·포크·판매·서비스·교육·납품 **가능**. 단, 라이선스·**저작권 고지 보존**·**변경 명시**·`NOTICE` 동봉 필요. **보증 없음**·상표권 미부여.
- **면책:** "있는 그대로(AS IS)" 제공, 사용 결과·손해에 **보증·책임 없음**. "100% 안전" 보장 안 함.
- **상표:** "Claude Code"·"Claude"=Anthropic, "Codex"=OpenAI 상표. 이 도구는 **호환 표시(명목적 사용)** 일 뿐 **공식 제휴·후원 아님**.
- **생성물 소유권:** 문진으로 만든 `CLAUDE.md`·`AGENTS.md`는 **사용자 것**이에요.

> 전체 라이선스·저작권·상업적 사용 조건은 `README.md` 16번 항목과 `LICENSE`·`NOTICE` 파일을 보세요.

---

> 끝까지 따라오셨어요, 멋져요! 👏 더 짧은 요약·명령어·아키텍처·전체 라이선스는 `README.md` 에 있습니다. (English: `README.en.md`, `GUIDE.en.md`)
