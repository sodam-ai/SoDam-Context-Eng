# SoDamContext
node_modules/

# 백업·리포트(런타임 산출물) — 비밀키 복사본이 git에 올라가는 사고 방지 (PRD 07 §2.3 C2)
.sodamcontext/

# 개발 내부 문서
DEVELOPMENT.md
TESTING.md

# 내부 기획·진행 문서 (공개 배포 제외 — 내부 전략 노출 방지, PRD 10·11)
.PRD/
CHECKPOINT.md
tasks/

# 베타 진행자용 준비물 (가짜 비밀키 fixture 포함 — 배포·git 추적 제외, PRD 12_BETA §3 위생)
beta-kit/

# 로그
*.log

# 로컬 도구 상태·캐시·크래시덤프 (로컬 전용 — push 제외)
.omc/
.complexity-log.md
.todos-report.md/
*.stackdump

# 플러그인 자동 생성 로컬 파일 (사용자별 설정 — push 제외)
.plugin-config/
.pair-programming-session.md

# 절대 푸시 금지 메모(루트)
.이 문서는 반드시 깃허브 저장소에 푸시 하지 않기.md

# SoDamContext 백업 (비밀키 포함 가능, 커밋 금지)
.sodamcontext/backups/
